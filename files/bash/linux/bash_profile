#!/usr/bin/env bash
# Resolve the real path of this script (follow symlinks) then compute its directory.
script_path="$(readlink -f "${BASH_SOURCE[0]}" 2>/dev/null || printf '%s' "${BASH_SOURCE[0]}")"
script_dir="$(cd "$(dirname "$script_path")" && pwd)"
source "$script_dir/../common/bash_profile"
